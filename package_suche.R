library("faraway")
View(nepali) # Viele verschiedene Daten, 1000 Entries, bisschen weird ID
?nepali
summary(nepali)
install.packages("MASS")
library("MASS")
View(quine) # Recht simpel, aber machbar
?quine
View(Boston) # Geiler Datensatz aber catcht nicht wirklich
?Boston
View(survey)

#unsere Favs waren neapli und Survey aber Survey haben wir jetzt genommen
#UScereal kann man auch machen

View(Boston)
summary(Boston)
?Boston
function(v) { uniqv <- unique(v)}
function(v)
